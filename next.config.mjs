/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.sanity.io'], // Replace with the domain(s) serving your images
      },
};

export default nextConfig;
